Running tests
=============

You can run all unittests from command line after having
downloaded the source code from
`GitHub <https://github.com/pietrobarbiero/deep-logic>`__:

.. code:: bash

    $ git clone https://github.com/pietrobarbiero/deep-logic.git
    $ cd ./deep_logic

You can use either python:

.. code:: bash

    $ python -m unittest discover

or coverage:

.. code:: bash

    $ coverage run -m unittest discover